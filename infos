# Commandes pr setup:

-	installation docker

		https://docs.docker.com/engine/install/ubuntu/


-	groupe docker

		https://docs.docker.com/engine/install/linux-postinstall (getent group pr voir les groupes)


-	tester si docker fonctionne:

	docker run hello-world

-	docker image
	
		https://www.geeksforgeeks.org/devops/what-is-docker-image/


ouvrir port 443 NGINX, seul point d'entrée

fabriquer image: docker build "path"
				 docker build -t "name" "path"

lancer image:	 docker run "name given with -t"
	check running img: docker ps